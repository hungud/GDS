-- create table
CREATE TABLE GDS_INTERFACE_PROVEEDOR
(
  PSEUDO_CONSULTA         varchar2(12) NOT NULL,
  ESQUEMA_CONSULTA        varchar2(30) NOT NULL,
  PROVEEDOR_PTA_CONSULTA  number NOT NULL,
  REFERENCIA_PTA_CONSULTA number NOT NULL,
  PSEUDO_EMISION          varchar2(12) NOT NULL,
  ESQUEMA_EMISION         varchar2(20),
  PROVEEDOR_PTA_EMISION   number,
  REFERENCIA_PTA_EMISION  number,
  DOBLE_INTERFACE         number(1) NOT NULL
);

-- create/recreate primary, unique and foreign key constraints 
ALTER TABLE GDS_INTERFACE_PROVEEDOR
  ADD CONSTRAINT GDS_INTERFACE_PROVEEDOR_PK PRIMARY KEY (PSEUDO_CONSULTA, PSEUDO_EMISION);
